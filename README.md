# assesment-1-partB

This is for part B of the assignment introudtion to c++

in this script it contains the scripts to:

6) linear search
searching an array by going through the index and atempting to match the numbers inputed to one in the array.
   
7) bubble sort
sorting an array by swapping two numbers with it being smallest on the left and largest numbers being on the right of the array. 
   
8) binary search
searching through a sorted array by using low, mid and high points to halve the search area each time. 
    
9) pointers swap
using pointers to swap two numbers using pointers and temp number 
    
10) mob battle
create a srcipt that make to structs deal damage to one another then declear a winner when on dies  
